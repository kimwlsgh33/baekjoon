#include <iostream>
using namespace std;

int main(void) {

  long A, B, C; // 세 개의 long형 정수 변수 A, B, C를 선언합니다.

  // 사용자로부터 세 개의 정수를 입력받아 변수 A, B, C에 저장합니다.
  cin >> A >> B >> C;

  // 변수 A, B, C의 값을 모두 더한 결과를 출력합니다.
  cout << A + B + C << endl;

  return 0;
}
